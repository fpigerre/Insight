<nav class="navbar navbar-default navbar-static-top navbar-collapse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Insight', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-main">
      <ul class="nav navbar-nav links">
        <% if request.env['PATH_INFO'] == '/dash' %>
            <li class="active">
        <% else %>
            <li>
        <% end %>
        <%= link_to 'Home', root_path %></li>
        <% if request.env['PATH_INFO'] == '/cctv' %>
            <li class="active">
        <% else %>
            <li>
        <% end %>
        <%= link_to 'CCTV', cctv_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right links">
        <li class="nav-icon"><%= link_to('', edit_user_registration_path, class: 'fa fa-cog') %></li>
        <li class="nav-icon"><%= link_to('', destroy_user_session_path, :method => :delete, class: 'fa fa-sign-out') %></li>
      </ul>
    </div>
  </div>
</nav>